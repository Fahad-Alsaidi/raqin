// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package repo

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// LineRevisionReaction is an object representing the database table.
type LineRevisionReaction struct {
	ID             int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	LineRevisionID int       `boil:"line_revision_id" json:"line_revision_id" toml:"line_revision_id" yaml:"line_revision_id"`
	ReactorID      int       `boil:"reactor_id" json:"reactor_id" toml:"reactor_id" yaml:"reactor_id"`
	Reaction       string    `boil:"reaction" json:"reaction" toml:"reaction" yaml:"reaction"`
	CreatedAt      time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt      time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt      time.Time `boil:"deleted_at" json:"deleted_at" toml:"deleted_at" yaml:"deleted_at"`

	R *lineRevisionReactionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lineRevisionReactionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var LineRevisionReactionColumns = struct {
	ID             string
	LineRevisionID string
	ReactorID      string
	Reaction       string
	CreatedAt      string
	UpdatedAt      string
	DeletedAt      string
}{
	ID:             "id",
	LineRevisionID: "line_revision_id",
	ReactorID:      "reactor_id",
	Reaction:       "reaction",
	CreatedAt:      "created_at",
	UpdatedAt:      "updated_at",
	DeletedAt:      "deleted_at",
}

// Generated where

var LineRevisionReactionWhere = struct {
	ID             whereHelperint
	LineRevisionID whereHelperint
	ReactorID      whereHelperint
	Reaction       whereHelperstring
	CreatedAt      whereHelpertime_Time
	UpdatedAt      whereHelpertime_Time
	DeletedAt      whereHelpertime_Time
}{
	ID:             whereHelperint{field: "`line_revision_reaction`.`id`"},
	LineRevisionID: whereHelperint{field: "`line_revision_reaction`.`line_revision_id`"},
	ReactorID:      whereHelperint{field: "`line_revision_reaction`.`reactor_id`"},
	Reaction:       whereHelperstring{field: "`line_revision_reaction`.`reaction`"},
	CreatedAt:      whereHelpertime_Time{field: "`line_revision_reaction`.`created_at`"},
	UpdatedAt:      whereHelpertime_Time{field: "`line_revision_reaction`.`updated_at`"},
	DeletedAt:      whereHelpertime_Time{field: "`line_revision_reaction`.`deleted_at`"},
}

// LineRevisionReactionRels is where relationship names are stored.
var LineRevisionReactionRels = struct {
	LineRevision string
	Reactor      string
}{
	LineRevision: "LineRevision",
	Reactor:      "Reactor",
}

// lineRevisionReactionR is where relationships are stored.
type lineRevisionReactionR struct {
	LineRevision *LineRevision `boil:"LineRevision" json:"LineRevision" toml:"LineRevision" yaml:"LineRevision"`
	Reactor      *User         `boil:"Reactor" json:"Reactor" toml:"Reactor" yaml:"Reactor"`
}

// NewStruct creates a new relationship struct
func (*lineRevisionReactionR) NewStruct() *lineRevisionReactionR {
	return &lineRevisionReactionR{}
}

// lineRevisionReactionL is where Load methods for each relationship are stored.
type lineRevisionReactionL struct{}

var (
	lineRevisionReactionAllColumns            = []string{"id", "line_revision_id", "reactor_id", "reaction", "created_at", "updated_at", "deleted_at"}
	lineRevisionReactionColumnsWithoutDefault = []string{"line_revision_id", "reactor_id"}
	lineRevisionReactionColumnsWithDefault    = []string{"id", "reaction", "created_at", "updated_at", "deleted_at"}
	lineRevisionReactionPrimaryKeyColumns     = []string{"id"}
)

type (
	// LineRevisionReactionSlice is an alias for a slice of pointers to LineRevisionReaction.
	// This should generally be used opposed to []LineRevisionReaction.
	LineRevisionReactionSlice []*LineRevisionReaction
	// LineRevisionReactionHook is the signature for custom LineRevisionReaction hook methods
	LineRevisionReactionHook func(context.Context, boil.ContextExecutor, *LineRevisionReaction) error

	lineRevisionReactionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lineRevisionReactionType                 = reflect.TypeOf(&LineRevisionReaction{})
	lineRevisionReactionMapping              = queries.MakeStructMapping(lineRevisionReactionType)
	lineRevisionReactionPrimaryKeyMapping, _ = queries.BindMapping(lineRevisionReactionType, lineRevisionReactionMapping, lineRevisionReactionPrimaryKeyColumns)
	lineRevisionReactionInsertCacheMut       sync.RWMutex
	lineRevisionReactionInsertCache          = make(map[string]insertCache)
	lineRevisionReactionUpdateCacheMut       sync.RWMutex
	lineRevisionReactionUpdateCache          = make(map[string]updateCache)
	lineRevisionReactionUpsertCacheMut       sync.RWMutex
	lineRevisionReactionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var lineRevisionReactionBeforeInsertHooks []LineRevisionReactionHook
var lineRevisionReactionBeforeUpdateHooks []LineRevisionReactionHook
var lineRevisionReactionBeforeDeleteHooks []LineRevisionReactionHook
var lineRevisionReactionBeforeUpsertHooks []LineRevisionReactionHook

var lineRevisionReactionAfterInsertHooks []LineRevisionReactionHook
var lineRevisionReactionAfterSelectHooks []LineRevisionReactionHook
var lineRevisionReactionAfterUpdateHooks []LineRevisionReactionHook
var lineRevisionReactionAfterDeleteHooks []LineRevisionReactionHook
var lineRevisionReactionAfterUpsertHooks []LineRevisionReactionHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *LineRevisionReaction) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *LineRevisionReaction) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *LineRevisionReaction) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *LineRevisionReaction) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *LineRevisionReaction) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *LineRevisionReaction) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *LineRevisionReaction) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *LineRevisionReaction) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *LineRevisionReaction) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lineRevisionReactionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddLineRevisionReactionHook registers your hook function for all future operations.
func AddLineRevisionReactionHook(hookPoint boil.HookPoint, lineRevisionReactionHook LineRevisionReactionHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		lineRevisionReactionBeforeInsertHooks = append(lineRevisionReactionBeforeInsertHooks, lineRevisionReactionHook)
	case boil.BeforeUpdateHook:
		lineRevisionReactionBeforeUpdateHooks = append(lineRevisionReactionBeforeUpdateHooks, lineRevisionReactionHook)
	case boil.BeforeDeleteHook:
		lineRevisionReactionBeforeDeleteHooks = append(lineRevisionReactionBeforeDeleteHooks, lineRevisionReactionHook)
	case boil.BeforeUpsertHook:
		lineRevisionReactionBeforeUpsertHooks = append(lineRevisionReactionBeforeUpsertHooks, lineRevisionReactionHook)
	case boil.AfterInsertHook:
		lineRevisionReactionAfterInsertHooks = append(lineRevisionReactionAfterInsertHooks, lineRevisionReactionHook)
	case boil.AfterSelectHook:
		lineRevisionReactionAfterSelectHooks = append(lineRevisionReactionAfterSelectHooks, lineRevisionReactionHook)
	case boil.AfterUpdateHook:
		lineRevisionReactionAfterUpdateHooks = append(lineRevisionReactionAfterUpdateHooks, lineRevisionReactionHook)
	case boil.AfterDeleteHook:
		lineRevisionReactionAfterDeleteHooks = append(lineRevisionReactionAfterDeleteHooks, lineRevisionReactionHook)
	case boil.AfterUpsertHook:
		lineRevisionReactionAfterUpsertHooks = append(lineRevisionReactionAfterUpsertHooks, lineRevisionReactionHook)
	}
}

// One returns a single lineRevisionReaction record from the query.
func (q lineRevisionReactionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*LineRevisionReaction, error) {
	o := &LineRevisionReaction{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "repo: failed to execute a one query for line_revision_reaction")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all LineRevisionReaction records from the query.
func (q lineRevisionReactionQuery) All(ctx context.Context, exec boil.ContextExecutor) (LineRevisionReactionSlice, error) {
	var o []*LineRevisionReaction

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "repo: failed to assign all query results to LineRevisionReaction slice")
	}

	if len(lineRevisionReactionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all LineRevisionReaction records in the query.
func (q lineRevisionReactionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "repo: failed to count line_revision_reaction rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q lineRevisionReactionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "repo: failed to check if line_revision_reaction exists")
	}

	return count > 0, nil
}

// LineRevision pointed to by the foreign key.
func (o *LineRevisionReaction) LineRevision(mods ...qm.QueryMod) lineRevisionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.LineRevisionID),
	}

	queryMods = append(queryMods, mods...)

	query := LineRevisions(queryMods...)
	queries.SetFrom(query.Query, "`line_revision`")

	return query
}

// Reactor pointed to by the foreign key.
func (o *LineRevisionReaction) Reactor(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.ReactorID),
	}

	queryMods = append(queryMods, mods...)

	query := Users(queryMods...)
	queries.SetFrom(query.Query, "`user`")

	return query
}

// LoadLineRevision allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (lineRevisionReactionL) LoadLineRevision(ctx context.Context, e boil.ContextExecutor, singular bool, maybeLineRevisionReaction interface{}, mods queries.Applicator) error {
	var slice []*LineRevisionReaction
	var object *LineRevisionReaction

	if singular {
		object = maybeLineRevisionReaction.(*LineRevisionReaction)
	} else {
		slice = *maybeLineRevisionReaction.(*[]*LineRevisionReaction)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &lineRevisionReactionR{}
		}
		args = append(args, object.LineRevisionID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &lineRevisionReactionR{}
			}

			for _, a := range args {
				if a == obj.LineRevisionID {
					continue Outer
				}
			}

			args = append(args, obj.LineRevisionID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`line_revision`),
		qm.WhereIn(`line_revision.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load LineRevision")
	}

	var resultSlice []*LineRevision
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice LineRevision")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for line_revision")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for line_revision")
	}

	if len(lineRevisionReactionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.LineRevision = foreign
		if foreign.R == nil {
			foreign.R = &lineRevisionR{}
		}
		foreign.R.LineRevisionReactions = append(foreign.R.LineRevisionReactions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.LineRevisionID == foreign.ID {
				local.R.LineRevision = foreign
				if foreign.R == nil {
					foreign.R = &lineRevisionR{}
				}
				foreign.R.LineRevisionReactions = append(foreign.R.LineRevisionReactions, local)
				break
			}
		}
	}

	return nil
}

// LoadReactor allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (lineRevisionReactionL) LoadReactor(ctx context.Context, e boil.ContextExecutor, singular bool, maybeLineRevisionReaction interface{}, mods queries.Applicator) error {
	var slice []*LineRevisionReaction
	var object *LineRevisionReaction

	if singular {
		object = maybeLineRevisionReaction.(*LineRevisionReaction)
	} else {
		slice = *maybeLineRevisionReaction.(*[]*LineRevisionReaction)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &lineRevisionReactionR{}
		}
		args = append(args, object.ReactorID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &lineRevisionReactionR{}
			}

			for _, a := range args {
				if a == obj.ReactorID {
					continue Outer
				}
			}

			args = append(args, obj.ReactorID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`user`),
		qm.WhereIn(`user.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for user")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user")
	}

	if len(lineRevisionReactionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Reactor = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.ReactorLineRevisionReactions = append(foreign.R.ReactorLineRevisionReactions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ReactorID == foreign.ID {
				local.R.Reactor = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.ReactorLineRevisionReactions = append(foreign.R.ReactorLineRevisionReactions, local)
				break
			}
		}
	}

	return nil
}

// SetLineRevision of the lineRevisionReaction to the related item.
// Sets o.R.LineRevision to related.
// Adds o to related.R.LineRevisionReactions.
func (o *LineRevisionReaction) SetLineRevision(ctx context.Context, exec boil.ContextExecutor, insert bool, related *LineRevision) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `line_revision_reaction` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"line_revision_id"}),
		strmangle.WhereClause("`", "`", 0, lineRevisionReactionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.LineRevisionID = related.ID
	if o.R == nil {
		o.R = &lineRevisionReactionR{
			LineRevision: related,
		}
	} else {
		o.R.LineRevision = related
	}

	if related.R == nil {
		related.R = &lineRevisionR{
			LineRevisionReactions: LineRevisionReactionSlice{o},
		}
	} else {
		related.R.LineRevisionReactions = append(related.R.LineRevisionReactions, o)
	}

	return nil
}

// SetReactor of the lineRevisionReaction to the related item.
// Sets o.R.Reactor to related.
// Adds o to related.R.ReactorLineRevisionReactions.
func (o *LineRevisionReaction) SetReactor(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `line_revision_reaction` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"reactor_id"}),
		strmangle.WhereClause("`", "`", 0, lineRevisionReactionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ReactorID = related.ID
	if o.R == nil {
		o.R = &lineRevisionReactionR{
			Reactor: related,
		}
	} else {
		o.R.Reactor = related
	}

	if related.R == nil {
		related.R = &userR{
			ReactorLineRevisionReactions: LineRevisionReactionSlice{o},
		}
	} else {
		related.R.ReactorLineRevisionReactions = append(related.R.ReactorLineRevisionReactions, o)
	}

	return nil
}

// LineRevisionReactions retrieves all the records using an executor.
func LineRevisionReactions(mods ...qm.QueryMod) lineRevisionReactionQuery {
	mods = append(mods, qm.From("`line_revision_reaction`"))
	return lineRevisionReactionQuery{NewQuery(mods...)}
}

// FindLineRevisionReaction retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLineRevisionReaction(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*LineRevisionReaction, error) {
	lineRevisionReactionObj := &LineRevisionReaction{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `line_revision_reaction` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, lineRevisionReactionObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "repo: unable to select from line_revision_reaction")
	}

	return lineRevisionReactionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *LineRevisionReaction) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("repo: no line_revision_reaction provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lineRevisionReactionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	lineRevisionReactionInsertCacheMut.RLock()
	cache, cached := lineRevisionReactionInsertCache[key]
	lineRevisionReactionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			lineRevisionReactionAllColumns,
			lineRevisionReactionColumnsWithDefault,
			lineRevisionReactionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(lineRevisionReactionType, lineRevisionReactionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lineRevisionReactionType, lineRevisionReactionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `line_revision_reaction` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `line_revision_reaction` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `line_revision_reaction` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, lineRevisionReactionPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "repo: unable to insert into line_revision_reaction")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == lineRevisionReactionMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "repo: unable to populate default values for line_revision_reaction")
	}

CacheNoHooks:
	if !cached {
		lineRevisionReactionInsertCacheMut.Lock()
		lineRevisionReactionInsertCache[key] = cache
		lineRevisionReactionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the LineRevisionReaction.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *LineRevisionReaction) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	lineRevisionReactionUpdateCacheMut.RLock()
	cache, cached := lineRevisionReactionUpdateCache[key]
	lineRevisionReactionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			lineRevisionReactionAllColumns,
			lineRevisionReactionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("repo: unable to update line_revision_reaction, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `line_revision_reaction` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, lineRevisionReactionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lineRevisionReactionType, lineRevisionReactionMapping, append(wl, lineRevisionReactionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "repo: unable to update line_revision_reaction row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "repo: failed to get rows affected by update for line_revision_reaction")
	}

	if !cached {
		lineRevisionReactionUpdateCacheMut.Lock()
		lineRevisionReactionUpdateCache[key] = cache
		lineRevisionReactionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q lineRevisionReactionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "repo: unable to update all for line_revision_reaction")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "repo: unable to retrieve rows affected for line_revision_reaction")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LineRevisionReactionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("repo: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lineRevisionReactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `line_revision_reaction` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lineRevisionReactionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "repo: unable to update all in lineRevisionReaction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "repo: unable to retrieve rows affected all in update all lineRevisionReaction")
	}
	return rowsAff, nil
}

var mySQLLineRevisionReactionUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *LineRevisionReaction) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("repo: no line_revision_reaction provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lineRevisionReactionColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLLineRevisionReactionUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lineRevisionReactionUpsertCacheMut.RLock()
	cache, cached := lineRevisionReactionUpsertCache[key]
	lineRevisionReactionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			lineRevisionReactionAllColumns,
			lineRevisionReactionColumnsWithDefault,
			lineRevisionReactionColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			lineRevisionReactionAllColumns,
			lineRevisionReactionPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("repo: unable to upsert line_revision_reaction, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`line_revision_reaction`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `line_revision_reaction` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(lineRevisionReactionType, lineRevisionReactionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lineRevisionReactionType, lineRevisionReactionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "repo: unable to upsert for line_revision_reaction")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == lineRevisionReactionMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(lineRevisionReactionType, lineRevisionReactionMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "repo: unable to retrieve unique values for line_revision_reaction")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "repo: unable to populate default values for line_revision_reaction")
	}

CacheNoHooks:
	if !cached {
		lineRevisionReactionUpsertCacheMut.Lock()
		lineRevisionReactionUpsertCache[key] = cache
		lineRevisionReactionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single LineRevisionReaction record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LineRevisionReaction) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("repo: no LineRevisionReaction provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lineRevisionReactionPrimaryKeyMapping)
	sql := "DELETE FROM `line_revision_reaction` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "repo: unable to delete from line_revision_reaction")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "repo: failed to get rows affected by delete for line_revision_reaction")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q lineRevisionReactionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("repo: no lineRevisionReactionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "repo: unable to delete all from line_revision_reaction")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "repo: failed to get rows affected by deleteall for line_revision_reaction")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LineRevisionReactionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(lineRevisionReactionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lineRevisionReactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `line_revision_reaction` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lineRevisionReactionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "repo: unable to delete all from lineRevisionReaction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "repo: failed to get rows affected by deleteall for line_revision_reaction")
	}

	if len(lineRevisionReactionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LineRevisionReaction) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindLineRevisionReaction(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LineRevisionReactionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := LineRevisionReactionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lineRevisionReactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `line_revision_reaction`.* FROM `line_revision_reaction` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lineRevisionReactionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "repo: unable to reload all in LineRevisionReactionSlice")
	}

	*o = slice

	return nil
}

// LineRevisionReactionExists checks if the LineRevisionReaction row exists.
func LineRevisionReactionExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `line_revision_reaction` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "repo: unable to check if line_revision_reaction exists")
	}

	return exists, nil
}
